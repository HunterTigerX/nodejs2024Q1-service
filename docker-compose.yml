version: '3'
services:
  postgresql:
    build: ./postgresql
    image: postgres-huntertigerx
    ports:
      - "5432:5432"

  myapplication:
    build: 
          context: .
          dockerfile: Dockerfile
    image: homelibrary-huntertigerx
    ports:
      - "4000:4000"